<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
        <record model="ir.ui.view" id="dependencies_graph.list">
          <field name="name">dependencies_graph list</field>
          <field name="model">dependencies_graph.dependencies_graph</field>
          <field name="arch" type="xml">
            <tree>
              <field name="name"/>
              <field name="value"/>
              <field name="value2"/>
            </tree>
          </field>
        </record>
        -->

        <!-- actions opening views on models -->
        <!--
        <record model="ir.actions.act_window" id="dependencies_graph.action_window">
          <field name="name">dependencies_graph window</field>
          <field name="res_model">dependencies_graph.dependencies_graph</field>
          <field name="view_mode">tree,form</field>
        </record>
        -->

        <!-- server action to the one above -->
        <!--
        <record model="ir.actions.server" id="dependencies_graph.action_server">
          <field name="name">dependencies_graph server</field>
          <field name="model_id" ref="model_dependencies_graph_dependencies_graph"/>
          <field name="code">
            action = {
              "type": "ir.actions.act_window",
              "view_mode": "tree,form",
              "res_model": self._name,
            }
          </field>
        </record>
        -->

        <!-- Top menu item -->
        <!--
        <menuitem name="dependencies_graph" id="dependencies_graph.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
        <menuitem name="Menu 1" id="dependencies_graph.menu_1" parent="dependencies_graph.menu_root"/>
        <menuitem name="Menu 2" id="dependencies_graph.menu_2" parent="dependencies_graph.menu_root"/>
        -->
        <!-- actions -->
        <!--
        <menuitem name="List" id="dependencies_graph.menu_1_list" parent="dependencies_graph.menu_1"
                  action="dependencies_graph.action_window"/>
        <menuitem name="Server to list" id="dependencies_graph" parent="dependencies_graph.menu_2"
                  action="dependencies_graph.action_server"/>
        -->
    </data>
</odoo>